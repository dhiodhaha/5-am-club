# Discord Bot Configuration
DISCORD_TOKEN=your_discord_bot_token_here
DISCORD_CLIENT_ID=your_discord_client_id_here

# Neon PostgreSQL Database URL
DATABASE_URL=postgresql://username:password@ep-xxx-xxx-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require

# ============================================================
# OPTIONAL: For development/testing only
# ============================================================
# DISCORD_GUILD_ID=your_test_server_id
#
# If set: Commands deploy instantly to that server only (dev mode)
# If NOT set: Commands deploy globally to ALL servers (production)
#
# For production, leave DISCORD_GUILD_ID commented out!
# ============================================================

# NOTE: Channel and timezone are configured per-server via /setup command
# Each server admin runs:
#   /setup channel #channel-name  - Set the 5AM channel
#   /setup timezone Asia/Jakarta  - Set the timezone
#   /setup status                 - View current config
